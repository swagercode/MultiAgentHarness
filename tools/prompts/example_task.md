# Example Task

Make one small, verifiable improvement to the target repository.

Requirements:

- Keep the change bounded.
- Preserve existing architecture and tests.
- Run the configured verification commands.
- Report `BLOCKED` only for user-only blockers such as authentication, credentials, license acceptance, account access, or permissions the agent cannot grant.
